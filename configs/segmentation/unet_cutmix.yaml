device: cuda
seed: 0
dataset:
  root: ''
  download: false
  image_size: 128
train:
  batch_size: 16
  epochs: 30
  learning_rate: 0.001
  output_dir: experiments/segmentation/unet/cutmix_seed0
augmentation:
  condition: cutmix
  cutmix_alpha: 1.0
  cutmix_prob: 1.0
