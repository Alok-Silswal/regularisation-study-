device: cuda
seed: 0
dataset:
  root: ''
  download: false
  image_size: 128
train:
  batch_size: 16
  epochs: 30
  learning_rate: 0.001
  output_dir: experiments/segmentation/unet/baseline_seed0
augmentation:
  condition: baseline
